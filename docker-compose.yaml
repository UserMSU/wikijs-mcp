services:

  wikijs-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "wikijs-mcp"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    ports:
      - "3000:3000"